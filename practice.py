

# 2 + 3 -> 결과값




 # 인터프리터가 메모리로 이동하여, bar 라는 변수가 있는지 검색
 # 이미 선언된 변수가 있다.
 #  case 1)
    # bar 라는 이름을 가지는 변수를 선언
 # 없다
 #  case 2)


bar = 3

foo = 2

sum = bar + foo

sub = bar - foo

mul = bar * foo

div = bar / foo

print(f"sum : {sum}, sub: {sub}, mul: {mul}, div: {div}")